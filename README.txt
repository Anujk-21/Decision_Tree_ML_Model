=== Anuj Karoddeo ===

In this Model, I have applied Decision Tree ML algo on :
There is 4 rooms in our model and each room have 3 partitions and each partition has 2 rows & 2 columns.
We put some balls with specific colors in boxes with features like Room, Partitions, Rows, Columns.

Decision Tree is the most powerful and popular tool for classification and prediction. A Decision tree is a flowchart-like tree structure, where each internal node denotes a test on an attribute, each branch represents an outcome of the test, and each leaf node (terminal node) holds a class label. 

